# Configuration minimale pour Docker
port: 7880
log_level: info

rtc:
  tcp_port: 7881
  port_range_start: 40000
  port_range_end: 40100
  use_external_ip: false

redis:
  address: redis:6379

keys:
  # Clés API pour développement (à changer en production)
  devkey: devsecret123456789abcdef0123456789abcdef

# Configuration agent supprimée car non supportée par cette version de LiveKit

# Configuration TURN pour connectivité
turn:
  enabled: false
  # Désactivé pour tests locaux Docker